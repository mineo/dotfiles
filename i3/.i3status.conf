order += "battery 0"
order += "time"

general {
    colors = false
}
battery 0 {
    format = "%status %percentage %remaining"
    last_full_capacity = true
    format_down = ""
}

time {
    format = "%Y/%m/%d %H:%M"
}
