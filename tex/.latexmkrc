# Needed for the minted package
$latex = 'latex -shell-escape';
$pdflatex = 'pdflatex -shell-escape';
# I want PDF
$pdf_mode = 1;
# Try make to generate additional files
$use_make_for_missing_files = 1;
